<% if @errors %>
  <div id="errors">
        <h2><%= pluralize(@errors.count, "error") %> prevented this from saving:</h2>
        <ul>
          <% @errors.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
  </div>
<% end %>
